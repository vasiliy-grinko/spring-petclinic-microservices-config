server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:80/eureka/
---
spring:
  config:
    activate:
      on-profile: docker
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:80/eureka/
